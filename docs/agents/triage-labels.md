# Triage labels

For the default local Markdown tracker, canonical roles map to frontmatter status values:

- needs-triage
- needs-info
- ready-for-agent
- ready-for-human
- wontfix

The normal post-alignment spec status is `ready-for-agent`.
